<% provide(:title, "#{@institution.name}") %>

<div class="row">
  <div class="col-xs-10 col-xs-offset-1" style="text-align: center">
    <h1><%= link_to @institution.name, @institution.website, target: '_blank' %></h1>
  </div>
</div>

<%= render partial: 'users/list' %>
