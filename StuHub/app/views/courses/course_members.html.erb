<% provide(:title, "Members - #{@course.department.name}#{@course.number}") %>
<% provide(:main_title, "#{@course.department.name}#{@course.number} - #{@course.name}") %>

<%= render partial: 'users/list' %>
