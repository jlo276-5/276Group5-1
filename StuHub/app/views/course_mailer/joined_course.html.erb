<% provide(:email_title, "Joined Course : #{@course.department.name} #{@course.number} (#{@course.department.term.term_name})") %>

<p>You have joined the course <%= "#{@course.department.name} #{@course.number} - #{@course.name}" %> for the term <%= @course.department.term.term_name %>.</p>

<p>You can view the course at: <%= link_to "#{@course.department.name} #{@course.number}", @course %></p>

<p>You can view the course's info and set up your schedule at: <%= link_to "#{@course.department.name} #{@course.number} Info", get_info_course_url(@course) %></p>
