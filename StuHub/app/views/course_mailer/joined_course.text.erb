<% provide(:email_title, "Joined Course : #{@course.department.name} #{@course.number} (#{@course.department.term.term_name})") %>

You have joined the course <%= "#{@course.department.name} #{@course.number} - #{@course.name}" %> for the term <%= @course.department.term.term_name %>.

You can view the course at:

<%= course_url(@course) %>

You can view the course's info and set up your schedule at:

<%= get_info_course_url(@course) %>
