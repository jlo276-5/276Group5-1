<% provide(:email_title, "Dropped Course : #{@course.department.name} #{@course.number} (#{@course.department.term.term_name})") %>

You have dropped the course <%= "#{@course.department.name} #{@course.number} - #{@course.name}" %> for the term <%= @course.department.term.term_name %>.

You can readd the course at:

<%= get_info_course_url(@course) %>
