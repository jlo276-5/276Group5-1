<nav class="navbar navbar-inverse navbar-static-top navbar-mod">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-content" aria-expanded="false">
        <span class="icon-bar"></span>
        <!-- <span class="icon-bar"></span>
        <span class="icon-bar"></span> -->
      </button>
      <% if logged_in? %>
        <%= link_to 'Home', home_path, class: "navbar-brand" %>
      <% else %>
        <%= link_to 'Welcome', root_path, class: "navbar-brand" %>
      <% end %>
    </div>
    <div class="collapse navbar-collapse" id="navbar-content">
      <ul class="nav navbar-nav">
        <!-- Insert Links -->
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to 'Help', help_path %></li>
        <% if logged_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
              <%= gravatar_for current_user, size: 20, class: 'img-rounded' %>&nbsp;&nbsp;<%= current_user.name %> <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to 'User Profile', current_user %></li>
              <li><%= link_to 'Account Settings', edit_user_path(current_user) %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to 'Logout', logout_path, method: "delete" %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to 'About', about_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
